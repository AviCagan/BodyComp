import { PlaceholderScreen } from '@/components/PlaceholderScreen';
import { strings } from '@/strings';

export default function ProgressRoute() {
  return <PlaceholderScreen title={strings.tabs.progress} phase="Phase 3" />;
}
