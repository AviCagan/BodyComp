import { PlaceholderScreen } from '@/components/PlaceholderScreen';
import { strings } from '@/strings';

export default function HistoryRoute() {
  return <PlaceholderScreen title={strings.tabs.history} phase="Phase 2" />;
}
